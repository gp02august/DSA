class Solution {
public:
    void reverseString(vector<char>& s) {
        // int i=0, j=s.length()-1;
        // while(i<=j){
        //     char ch1=s[i], ch2=s[j];

        // }
        reverse(s.begin(), s.end());
    }
};